# Differences from upstream

This directory is the StarPivot share copy, not a live rebase target.

Pinned source: see [UPSTREAM.md](./UPSTREAM.md).

Kept from the Wuxie233 checkout at the time of the copy:

- Package name `@starpivot/dsh-agent-teams`
- Read-only role isolation (`src/roles.ts`)
- Barge-in mailbox delivery
- No greeting turn; first claimed task is the spawn prompt
- Optional per-member git worktrees
- Plugin-defect reports stay on `Wuxie233/dsh-plugin-agent-teams`
