{
  "name": "dsh-plugins-public",
  "private": true,
  "description": "Public out-of-tree DeepSeek Harness plugins. Each installable plugin lives in packages/<name>.",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  }
}
