# AGENTS.md

Org share copy of model-capabilities: `@starpivot/dsh-model-capabilities`.

Host daemon only. Never overwrite existing capability fields. Package name and mount `name` must stay `@starpivot/dsh-model-capabilities`.
