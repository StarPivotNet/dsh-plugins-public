# AGENTS.md

Org share copy of busy-enter-steer: `@starpivot/dsh-busy-enter-steer`.

Host daemon only. Seeds `ui-conversation.busyEnter=steer` when unset. Package name and mount `name` must stay `@starpivot/dsh-busy-enter-steer`.
